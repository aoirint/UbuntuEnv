# UbuntuEnv

This mounts `$HOME/Projects` to `/home/vagrant/Projects`.

```bash
# Start VM
vagrant up

# Connect with ssh
vagrant ssh

# Destroy VM
vagrant destroy

# Stop VM
vagrant halt

# Reload Vagrantfile and restart VM
vagrant reload
```

